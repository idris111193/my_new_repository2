команды на 1 раз
git init (инициализация проекта(гит видит ваш проект))
git remote add origin - ссылка на ваш репозиторий, привязывает ваш проект к репозитории


регулярные команды
git add . (добавляет ваш файл в гит)
git commit -m 'сохраняет файл в гите'
git push origin master (отправляет ваш проект в ваш репозитории)


полезные команды
git rm -r --cached (name file) удаляет файл из репозиторий
git clone link (клонирует чужие репозитории)
git romote remove origin (отвязывает от репозитория)
